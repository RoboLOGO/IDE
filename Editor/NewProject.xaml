<Window x:Class="Editor.NewProject"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Vászon beállítása" Height="300" Width="300">
    <Grid Margin="5">
        <Grid.Resources>
            <Style TargetType="{x:Type TextBlock}">
                <Setter Property="Padding" Value="5" />
            </Style>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="Vászon szélessége: " />
        <TextBox Margin="5" Name="canvasWidth" Grid.Column="1" />
        <TextBlock Grid.Row="1" Text="Vászon magassága: " />
        <TextBox Margin="5" Name="canvasHeight" Grid.Row="1" Grid.Column="1" />
        <TextBlock Grid.Row="2" Text="Mentés helye: " />
        <Button Width="70" Height="23" Grid.Column="1" Grid.Row="2" Content="Tallózás.." Click="Button_Click_1" />
        <Button Width="70" Height="23" Margin="0, 20, 0, 0" Grid.Row="3" Grid.Column="1" Content="Mehet!" Click="Button_Click" />
    </Grid>
</Window>
