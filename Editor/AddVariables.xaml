<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="Editor.AddVariables"
        Title="Változók" Height="90" Width="300" ResizeMode="NoResize">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label Content="Név:" />
        <TextBox Grid.Column="1" x:Name="nameBox" Width="100"/>

        <Label Grid.Column="2" Content="Érték:" />
        <TextBox Grid.Column="3" x:Name="valueBox" Width="100"/>

        <Button Width="70" HorizontalAlignment="Right" Grid.Row="1" x:Name="AddButton" Content="Hozzáad" Grid.Column="3" Margin="0,5,10,-5" Click="AddButton_Click"/>
    </Grid>
</Window>
