<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="Editor.AddVariables"
        Height="90" Width="300" ResizeMode="NoResize" Title="{DynamicResource newvarheader}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="{DynamicResource name}" Name="nameText" FontWeight="Bold" />
        <TextBox Grid.Column="1" x:Name="txtnameBox" Width="100"/>

        <TextBlock Grid.Column="2" Text="{DynamicResource value}" Name="valueText" FontWeight="Bold" />
        <TextBox Grid.Column="3" x:Name="txtvalueBox" Width="100"/>

        <Button Width="70" HorizontalAlignment="Right" Grid.Row="1" x:Name="addButton" Content="{DynamicResource add}" Grid.Column="3" Margin="0,5,10,-5" Click="AddButton_Click"/>
    </Grid>
</Window>
